phase: 1
contract_id: contract-example-valid-v1
created_at: "2026-05-19T20:00:00Z"
status: COMMITTED

specification:
  title: "Example Valid Contract"
  description: "This contract passes all validation rules and can be used as a reference."
  success_criteria:
    - "Contract validates successfully"
    - "All required fields present"

inputs:
  param1:
    type: "str"
    required: true
    description: "Example input parameter"
    example: "test_value"

outputs:
  result:
    type: "dict"
    description: "Example output"
    example:
      status: "success"

constraints:
  - "Input must be non-empty"

assumptions:
  - "Schema validation is working"

acceptance_tests:
  - name: "test_example_valid"
    given: "Valid contract data"
    when: "Validation runs"
    then: "Returns valid=true"

defer_to_next_phase:
  - "Advanced features (deferred to Phase 2)"
